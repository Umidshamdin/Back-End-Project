@model List<Event>
@{
    ViewData["Title"] = "Index";
    int count = 1;
}


<div class="container">
    <div class="row">
        <div class="header d-flex justify-content-between mt-3">
            <div>
                <a class="btn btn-success" asp-action="Create">Create</a>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            Number
                        </th>
                        <th>
                            Image
                        </th>
                        <th>
                            Time
                        </th>
                        <th>
                            Header
                        </th>
                        <th>
                            DayTime
                        </th>
                        <th>
                            Location
                        </th>
                        <th>
                            Description
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var events in Model)
                    {
                    <tr>
                        <td>
                            @count
                        </td>
                        <td>
                            <img src="~/assets/img/event/@events.Image" style="width:50px;height:50px" alt="slider" />
                        </td>
                        <td>
                            @events.Time
                        </td>
                        <td>
                            @events.Header;
                        </td>
                        <td>
                            @events.DayTime;
                        </td>
                        <td>
                            @events.Location;
                        </td>
                        <td>
                            @events.Description;
                        </td>
                        <td>
                            <a class="btn btn-primary btn-sm" asp-action="Detail" asp-route-id="@events.Id">
                                Detail
                            </a>
                            <a class="btn btn-warning btn-sm" asp-action="Update" asp-route-id="@events.Id">
                                Update
                            </a>
                            <form style="display:inline-block" method="post" asp-action="Delete" asp-route-id="@events.Id">
                                <button class="btn btn-danger btn-sm" type="submit">
                                    Delete
                                </button>
                            </form>
                        </td>
                    </tr>
                        count++;
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

